package com.rdodo.springcore;

import org.springframework.beans.factory.BeanFactory;
import org.springframework.beans.factory.xml.XmlBeanFactory;
import org.springframework.context.ApplicationContext;
import org.springframework.context.support.ClassPathXmlApplicationContext;
import org.springframework.core.io.ClassPathResource;

import com.rdodo.springcore.model.Address;
import com.rdodo.springcore.model.Customer;

/**
 * Demo for Spring Core features
 * 
 * Bean Factory: 
 * 	1. It's a lazy loading concepts
 *  2. It will not create object util u call getBean() method
 *  
 * ApplicationContext:
 * 	1. It's eager loading.
 * 	2. It will create all object provided in xml file wihtout calling
 * 		getBean() method.
 *
 */
public class App 
{
    public static void main( String[] args )
    {
    	
    	
    	//BeanFactory beanFactory = new XmlBeanFactory(new ClassPathResource("spring.xml"));
    	
		/*
		 * Customer customer = beanFactory.getBean("customer", Customer.class); Address
		 * address = beanFactory.getBean("address", Address.class);
		 */    	
    	//System.out.println (customer);
    	
    	//ApplicationContext applicationContext = new ClassPathXmlApplicationContext("spring.xml"); 
    }
}
